# Force fresh build (cache-bust 2026-07-30)
[phases.setup]
nixPkgs = ["caddy"]

[phases.install]
cmds = ["echo 'No build required for static site'"]

[start]
cmd = "caddy run --config Caddyfile --adapter caddyfile"
